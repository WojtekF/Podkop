// The hardcoded stub identity every interaction acts as until real authentication exists —
// deliberately one of the five sample authors so own-content rules are observable in the
// running app (issue #13). Must match the backend's stub. Real auth replaces exactly this.
export const CURRENT_USER = 'ada_lovelace';
