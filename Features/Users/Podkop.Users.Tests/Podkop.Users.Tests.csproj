<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="coverlet.collector" Version="6.0.4" />
    <PackageReference Include="Microsoft.AspNetCore.Mvc.Testing" Version="10.0.9" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.14.1" />
    <PackageReference Include="xunit" Version="2.9.3" />
    <PackageReference Include="xunit.runner.visualstudio" Version="3.1.4" />
  </ItemGroup>

  <ItemGroup>
    <!-- The persistence specs (issue #88) build the slice's context from a host builder and from
         the design-time factory; neither opens a connection, so no container runtime is needed. -->
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="10.0.11" PrivateAssets="all" />
    <PackageReference Include="Microsoft.Extensions.Hosting" Version="10.0.11" />
    <!-- The seed specs run the insert-and-skip machinery on Sqlite: no schema fidelity is
         involved there, so ADR 0010's rejection of Sqlite for endpoint tests does not apply —
         the same carve-out Podkop.MigrationService.Tests takes. -->
    <PackageReference Include="Microsoft.EntityFrameworkCore.Sqlite" Version="10.0.11" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\..\Podkop.Server\Podkop.Server.csproj" />
    <ProjectReference Include="..\..\..\Shared\Podkop.Shared.Testing\Podkop.Shared.Testing.csproj" />
    <ProjectReference Include="..\Podkop.Users.Domain\Podkop.Users.Domain.csproj" />
    <ProjectReference Include="..\Podkop.Users.Infrastructure\Podkop.Users.Infrastructure.csproj" />
  </ItemGroup>

</Project>
